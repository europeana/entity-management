resources:
  - deployment.yaml

commonLabels:
  app: entity-management-api

# expects files to be in the same directory
secretGenerator:
  - name: entity-management-api-secret1
    files:
      - entitymanagement.user.properties
  - name: entity-management-api-secret2
    files:
      - zoho_import.properties